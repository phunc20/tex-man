- In math mode, certain characters like Greek characters
  do not get boldface when we apply `\mathbf`, e.g.
    - `$\mathbf{theta}$` gives $\mathbf{\theta}$
  
  In this case, use `\bm` instead
    - `$\bm{\theta}$` gives $\bm{\theta}$
